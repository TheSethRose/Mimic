# Kraken API Configuration
# 
# NOTE: This skill uses ONLY public endpoints and does NOT require API keys.
# Public endpoints used:
#   - /public/OHLC (OHLC data)
#   - /public/Ticker (ticker information)
#   - /public/AssetPairs (trading pairs)
#
# API keys are ONLY needed if you extend this skill to use private endpoints:
#   - Account balances (/private/Balance)
#   - Open orders (/private/OpenOrders)
#   - Trade history (/private/TradesHistory)
#   - Place/cancel orders (/private/AddOrder, /private/CancelOrder)
#
# To get API keys (if extending to private endpoints):
# 1. Login to Kraken: https://www.kraken.com
# 2. Go to Settings → API
# 3. Create new API key with appropriate permissions
# 4. Copy this file to .env and add your keys below

# Kraken API Key (leave empty for public endpoints only)
KRAKEN_API_KEY=

# Kraken Private Key (leave empty for public endpoints only)
KRAKEN_PRIVATE_KEY=

# API Configuration
# Base URL for Kraken REST API (do not change unless using testnet)
KRAKEN_API_URL=https://api.kraken.com

# Rate limiting (seconds between requests)
# Default: 0.5 (safe for public endpoints)
# Increase if you get rate limited (HTTP 429 errors)
RATE_LIMIT=0.5

# Default Analysis Parameters
# Momentum threshold (standard deviations)
MOMENTUM_THRESHOLD=2.0

# Volatility threshold (annualized %)
VOLATILITY_THRESHOLD=2.5

# RSI period (candles)
RSI_PERIOD=14

# Moving average periods
MA_FAST=12
MA_SLOW=26

# Default fetch parameters
DEFAULT_INTERVAL=60
DEFAULT_COUNT=100
